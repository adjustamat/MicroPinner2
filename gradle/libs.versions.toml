[versions]
agp = "8.5.2"
espressoCore = "3.0.2"
espressoIntents = "3.0.2"
runner = "1.0.2"
rules = "1.0.2"

[libraries]
appcompat = { group = "androidx.appcompat", name = "appcompat", version = "1.7.0" }
espresso-core = { group = "com.android.support.test.espresso", name = "espresso-core", version.ref = "espressoCore" }
espresso-intents = { group = "com.android.support.test.espresso", name = "espresso-intents", version.ref = "espressoIntents" }
runner = { group = "com.android.support.test", name = "runner", version.ref = "runner" }
rules = { group = "com.android.support.test", name = "rules", version.ref = "rules" }

[plugins]
android-application = { id = "com.android.application", version.ref = "agp" }

